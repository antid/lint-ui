{
  "name": "@lint-ui/rules",
  "version": "0.1.0",
  "description": "Accessibility and layout validation rules for Lint UI",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo",
    "test": "echo \"No tests yet\""
  },
  "dependencies": {
    "axe-core": "^4.8.3",
    "playwright": "^1.41.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.5",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
